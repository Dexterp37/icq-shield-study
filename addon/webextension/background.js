"use strict";

function runOnce() {
  // We don't do much here, since we don't really use non-legacy stuff.
  // We keep this file around to not mess with the template structure.
}

// actually start
runOnce()
